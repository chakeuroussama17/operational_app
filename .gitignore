# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Separate projects that live in this folder but are their OWN git repos
# (operational_app_landing_page / landing_page_dreaming_ball). Adding them
# here would commit broken submodule pointers with no content.
/landing_page_operational_app/
/landing_page_dreaming_ball/

# Flutter web build output (deployed to Firebase Hosting, not committed)
/build/
/.firebase/
# Standalone dashboard project — reads the spreadsheet, not the app backend.
# Kept out of the app's history like the landing pages; give it its own repo
# when it's ready to ship.
/operational dashboard/
